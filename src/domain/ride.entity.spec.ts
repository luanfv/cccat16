describe('RideEntity unit tests', () => {
	describe('create', () => {
		it.todo('SHOULD return new ride with status = requested');
	});

	describe('restore', () => {
		it.todo('SHOULD return the ride');
	});
});