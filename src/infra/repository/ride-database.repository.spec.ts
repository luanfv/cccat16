describe('RideDatabaseRepository integration tests', () => {
	describe('saveRide', () => {
		it.todo('SHOULD save the account in database');
	});

	describe('getRideById', () => {
		it.todo('SHOULD return the account from database');
	});

	describe('hasActiveRideByPassengerId', () => {
		describe('WHEN account has a active ride', () => {
			it.todo('SHOULD return true');
		});


		describe('WHEN account has a active ride', () => {
			it.todo('SHOULD return false');
		});
	});
});